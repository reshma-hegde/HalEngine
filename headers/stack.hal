fun pop(array a)
    var length = len(a);

    if length == 0
        print("Error: cannot pop from empty array");
        return a;
    fi
 
    a = realloc(a, length - 1);

    return a;
nuf


fun push(array a, var value)
    var length = len(a);
 
    a = realloc(a, length + 1);
 
    a[length] = value;

    return a; 
nuf