fun main()
    var timeline = 1;
    fork
        timeline = timeline + 1;
    or
        timeline = timeline * 3;
    or
        timeline = timeline +200;
    merge timelines;
    
    print("Possible timelines: ");
    print(timelines);
    
    return 0;
nuf
