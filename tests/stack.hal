load "stack";

fun main()
    array nums = [10, 20, 30, 40, 50];

    var removed = pop(nums);
    print("removed element: ", removed);
    print(nums);

    nums = push(nums, 99);

    print("\nAfter push:");
    print(nums);

    return 0;
nuf
