fun main()
    var pi = 3.14159265d;
    var angle = pi / 4d;   
    var cs = cosec(angle);
    var sc = sec(angle);
    var ct = cot(angle);

    print("angle:", angle, "cosec:", cs, " sec:", sc, " cot:", ct);
    return 0;
nuf
