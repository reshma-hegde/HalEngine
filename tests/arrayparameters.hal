fun adds(array a, var count)
    var total = 0;
    var i = 0;
    while i < count
        total += a[i];
        i = i + 1;
    elihw
    return total;
nuf

fun main()
    array a = [1, 2, 3, 4, 5];
    var b=5;
    var result = adds(a, b);
    return result;
nuf