fun insert(array arr, var pos, var value)
    var count = len(arr);

    realloc(arr, count + 1); 

   
    var i = count;
    while i > pos
        arr[i] = arr[i - 1];
        i = i - 1;
    elihw

   
    arr[pos] = value;
    return arr;
nuf

fun main()
    array nums = [10, 20, 30, 40];

    print("before");
    print(nums);

   
    nums=insert(nums, 1, 15);  
    nums=insert(nums, 4, 35);  
    nums=insert(nums, 0, 5);   

    print("after multiple insertions");
    print(nums);

    print("Length = ", len(nums));

    return 0;
nuf
