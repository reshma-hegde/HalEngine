fun main()
    array nums = [1, 2, 3, 4];
    var count = len(nums);
    var index = 2;

    print("count=", count);
    print("before");
    
    var i = 0;
    while i < count
        print(nums[i]);
        i = i + 1;
    elihw

    i = count - 1;
    while i >= index
        nums[i + 1] = nums[i];
        i = i - 1;
    elihw

    nums[index] = 278;
    count = count + 1;

    print("after");
    i = 0;
    while i < count
        print(nums[i]);
        i = i + 1;
    elihw

    print("count:", count);

    return 0;
nuf
