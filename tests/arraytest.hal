fun main()
    array nums = [1, 2, 3, 4];
    var index = 2;      
    var value = 278;    

    print("before count= ",len(nums));
    
    print(nums);

   
    realloc(nums, len(nums) + 1);

    var i = len(nums) - 1;
    while i > index
        nums[i] = nums[i - 1];
        i = i - 1;
    elihw
    nums[index] = value;

    print("after count= ",len(nums));
    print(nums);


    return 0;
nuf
