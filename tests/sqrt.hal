fun sqrts(var x)
    if x < 0
        print("Error: negative input");
        return 0;
    fi

    var guess = x / 2;
    var i = 0;
    while i < 20
        guess = (guess + x / guess) / 2;
        i = i + 1;
    elihw
    return guess;
nuf

fun main()
    var n=100;
    var result = sqrts(n);
    print("sqrt(", n, ") =", result); 
    return 0;
nuf
