fun main()
    
    file file_ptr = open("example.txt", "w");
    if file_ptr == 0
        print("Failed to open file for writing");
        return 0;
    fi
    
    write(file_ptr, "First line\n");
    write(file_ptr, "Hello from HalEngine!\n");
    write(file_ptr, "This is the second one\n");
    write(file_ptr, "Will this be read by the compiler?\n");
    write(file_ptr, "Ah yes it did.\n");

    close(file_ptr);

    file_ptr = open("example.txt", "r");
    if file_ptr == 0
        print("Failed to open file for reading");
        return -1;
    fi

   
    var buffer = read_line(file_ptr);
    print("File content: ", buffer);

    var buffers = read_lines(file_ptr);
    print("File content: \n", buffers);

    close(file_ptr);

    return 0;
nuf

